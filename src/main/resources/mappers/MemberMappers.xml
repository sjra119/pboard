<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="memberMappers">

	 <insert id="createMember" parameterType="com.jnProject.pboard.domain.Member">
	 	INSERT INTO Member(m_id, password, name) VALUES(#{m_id}, #{password}, #{name})
	 </insert>
</mapper>